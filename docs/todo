backup to db?

boards will need to be dumped and resynced on start, current users will need to be reconnected and readded to connectionCheck, as heroku has daily restarts >:(

handle downtime/dropped connection on user end

question mark

rewrite mouseclicks

rewrite updating discord messages

burying